# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment and secrets (never commit)
.env
.env.*
!.env*.example
!.env.example
public/retell-config.json
.secrets
.secrets.*
secrets.*
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx
*.jks
*.keystore
*.enc

# Editor and OS
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swo
*~

# Testing and coverage
coverage/
.nyc_output/
*.lcov
test-results/

# Vite / tooling cache
.vite/
.eslintcache

# Stale production HTML snapshot (built output lives in dist/)
prod-index.html

# Cache and temp
.cache/
.turbo/
*.tsbuildinfo

# Vercel local project metadata
.vercel

# ------------------------------------------------------------------------------
# Android / Gradle (native build)
# ------------------------------------------------------------------------------
**/.gradle/
android/.gradle/
android/.idea/
android/local.properties
android/.cxx/
android/.settings/
android/build/
android/app/build/
android/**/build/
android/**/.externalNativeBuild/

# Legacy TWA Android project (replaced by Capacitor) — keep locally only
android-twa-backup/

# Signing key backups — never commit
keystore-backup/

# Keystores and signing (never commit)
android/app/*.keystore
android/app/*.jks
android/key.properties

# Release artifacts (never commit)
android/app/release/
Library-Connekto-Release.aab
Library-Connekto-Release.apk
Library-Connekto-Debug.apk
*.aab
*.apk

# Optional: uncomment if you use iOS
# ios/Pods/
# ios/build/
# ios/*.xcworkspace/xcuserdata/
# ios/*.xcodeproj/xcuserdata/
# ios/*.xcodeproj/project.xcworkspace/xcuserdata/
